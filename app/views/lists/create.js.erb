$("#error_messages").remove()
<% if @list.errors.present? %>
  $("#new_list").append("<%= j(render('shared/error_messages', object: @list)) %>")
<% else %>
  $("#lists").append("<%= j(render partial: "tops/list", locals: { list: @list}) %>")  
  $("#list-modal").modal("hide");
<% end %>